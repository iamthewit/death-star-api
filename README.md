### Requirements

- PHP 7.2

## Setup

#### Create .env file

- `cp .env.example .env`
- Set PROJECT_ROOT_PATH in the .env file

#### Install the dependencies

- `composer install`

#### Run the tests

- `./vendor/bin/phpunit tests/`
